#!/usr/bin/env bash
# This script displays a detailed, user-oriented list of all running processes including those without a TTY and shows the process hierarchy.

ps -eo user,pid,ppid,%cpu,%mem,tty,cmd --forest
