#!/usr/bin/env bash
# This script displays all running processes in a hierarchical and user-friendly format, including those without a TTY.

ps -eo user,pid,%cpu,%mem,vsz,rss,tty,stat,start,time,cmd --forest
