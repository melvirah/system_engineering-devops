#!/usr/bin/env bash
# This script displays all currently running processes, including those without a TTY, in a user-friendly hierarchical format.

ps -eo user,pid,%cpu,%mem,vsz,rss,tty,stat,start,time,cmd --forest
